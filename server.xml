<server description="default servlet engine">

  <!-- Enable features -->
    <featureManager>
        <feature>servlet-3.0</feature>
    </featureManager>

    <httpEndpoint host="*" httpPort="8080" httpsPort="-1"/>
    
</server>
